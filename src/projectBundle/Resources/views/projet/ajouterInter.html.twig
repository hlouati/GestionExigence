{% extends ['userBundle:gestionUtilisateur:modal.html.twig']%}
{% block modal%}
    {% set listUser=''%} 
          <!-- Modal -->
          <script type="text/javascript" src="{{asset('js/jquery-1.10.2.js')}}"></script>
<script type="text/javascript" src="{{asset('js/bootstrap.min.js')}}"></script>
<link rel="stylesheet" type="text/css" href="{{asset('css/bootstrap.min.css')}}" >
<link rel="stylesheet" type="text/css" href="{{asset('css/bootstrap.css')}}" />
 <script type="text/javascript">
 $(document).ready(function() {
   $('#myModal').modal({
        show: true,
   })
 });
</script>
 <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-     labelledby="myModalLabel" aria-hidden="true">
   <div class="modal-dialog">
     <div class="modal-content">
       <div class="modal-header">
         <a href="{{ path('user_gestion')}}" class="close" >&times;</a>
         <h4 class="modal-title" id="myModalLabel">Ajouter intervenant</h4>
       </div>
       <div class="modal-body">
       {% form_theme form '::formLayout.html.twig' %}
     
        <form method="post" {{ form_enctype(form) }}>
            
        <div class="row">
        <div class="col-sm-3"> {{ form_label(form.user, "Choisir dans la liste:") }}</div>
        <div class="col-sm-6">  {{ form_widget(form.user,{ 'attr': {'cols': '60','rows':'30'} }) }}</div>
           </div><br/>  
        
     </div>
       <div class="modal-footer">
         <button type="submit" class="btn btn-primary">Ajouter</button>
         <a href="{{ path('Projet_inter', {'idprojet': app.request.get('idprojet') })}}" class="btn btn-default">Fermer</a>
       </div>
       {{ form_rest(form) }} 
         </form>
     </div><!-- /.modal-content -->
   </div><!-- /.modal-dialog -->
 </div><!-- /.modal -->
    {% endblock %}
        


 

   {# empty Twig template #}
