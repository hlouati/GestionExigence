{% extends ['userBundle:gestionUtilisateur:modal.html.twig']%}
{% block modal%}
    {% set listUser=''%} 
          <!-- Modal -->
          <script type="text/javascript" src="{{asset('js/jquery-1.10.2.js')}}"></script>
<script type="text/javascript" src="{{asset('js/bootstrap.min.js')}}"></script>
<link rel="stylesheet" type="text/css" href="{{asset('css/bootstrap.min.css')}}" >
<link rel="stylesheet" type="text/css" href="{{asset('css/bootstrap.css')}}" />
 <script type="text/javascript">
 $(document).ready(function() {
   $('#myModal').modal({
    show: true,
   })
 });
</script>
 <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-     labelledby="myModalLabel" aria-hidden="true">
   <div class="modal-dialog">
     <div class="modal-content">
       <div class="modal-header">
         <a href="{{ path('user_gestion')}}" class="close" >&times;</a>
         <h4 class="modal-title" id="myModalLabel">Liste des intervenant</h4>
       </div>
       <div class="modal-body">
    {% if is_granted('ROLE_CHEFPROJET') %} 
            <span>Ajouter un intervenant</span>
        
            <a href="{{ path('Projet_interCl', {'idprojet': app.request.get('idprojet') })}}"><span>Client</span></a>             
            <a href="{{ path('Projet_interCap', {'idprojet': app.request.get('idprojet') })}}">
                <span>Capgemeni</span></a>{% endif %}<br/>
     <br/>
       <table class="table table-condensed table-striped table-bordered table-hover no-margin">
    <thead>
      <tr>
        <th style="width:10%" class="hidden-phone">Nom</th>
        <th style="width:20%" class="hidden-phone">Prenom</th>
        <th style="width:10%" class="hidden-phone">Roles</th>
        <th style="width:10%" class="hidden-phone">Action</th>
     
      </tr>
    </thead>
    <tbody>
    
    {%for user in u.user%}
        <tr>
 
        <td class="hidden-phone">{{user.nom}}</td>
        <td class="hidden-phone">{{user.prenom}}</td>
        <td class="hidden-phone">{{user.Roles[0]}}</td>
        <td class="hidden-phone">
              <div class="btn-group">
            <button data-toggle="dropdown" class="btn btn-xs dropdown-toggle" data-original-title="" title="">
              Action 
              <span class="caret">
              </span>
            </button>
            <ul class="dropdown-menu pull-right">
              <li>
                <a href="#">Supprimer</a>
              </li>
            </ul>
          </div>
        </td>
        
      </tr>
    {%endfor%}
    
    </tbody>
  </table>
        
        
        
        
        
        
                </br>       
     </div>
       <div class="modal-footer">
         
         <a href="{{ path('project_list')}}" class="btn btn-default">Fermer</a>
       </div>

         </form>
     </div><!-- /.modal-content -->
   </div><!-- /.modal-dialog -->
 </div><!-- /.modal -->
    
        {% endblock%}


 

   